---
"@kitlangton/ghui": minor
---

Add "open pull request in editor" (`e`): suspend the TUI and launch a configurable command (e.g. `nvim` with diffview/octo, or `code`). Configure `editorCommand` and `repoPaths` in `config.json`, with gh-dash-style `{{...}}` substitutions and `owner/repo` path matching.
